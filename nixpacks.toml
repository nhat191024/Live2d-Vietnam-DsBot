[variables]
NODE_ENV = "production"

[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = [
  "corepack enable",
  "corepack prepare pnpm@11.15.1 --activate",
  "pnpm install --frozen-lockfile --prod"
]

[start]
cmd = "mkdir -p logs && pnpm start"
